spring:
  application:
    name: infinitepro-gateway

  cloud:
    nacos:
      config:
        server-addr: localhost:22000
        file-extension: yaml

  config:
    import:
      - optional:nacos:infinitepro-gateway.yaml