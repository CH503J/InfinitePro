spring:
  application:
    name: auth-service

  cloud:
    nacos:
      config:
        server-addr: localhost:22000
        file-extension: yaml

  config:
    import:
      - optional:nacos:auth-service.yaml